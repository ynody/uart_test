# UART コマンド通信モジュール単体テスト仕様書

## 1. 目的

本書は、UART コマンド通信モジュールのうち、Xilinx ライブラリおよび
ハードウェアに依存しないコアモジュールに対する単体テストを定義する。

テストは macOS を含む一般的な PC 上の C コンパイラで実行する。
実機 UART、Xilinx ヘッダ、Xilinx ドライバおよび実レジスタは使用しない。

## 2. 適用範囲

- `uart_comm_receive_char()` による 1 文字ずつの受信と行の完成
- `uart_comm_has_pending_line()` および `uart_comm_process_received_line()` による
  完成行の取り出し、コマンド解釈および振り分け
- レジスタ読み出し・書き込みコールバックの呼び出し
- 外部コマンドコールバックへの文字列転送
- 成功・エラー出力ラッパー、構文エラー、行バッファオーバーフローおよび
  受信キューオーバーフローの処理

Xilinx 依存のプラットフォームアダプタ、実 UART、実レジスタの動作確認は
本単体テストの対象外とする。

## 3. テスト構成

テスト対象のコアモジュールを、以下のテストダブルとリンクして実行する。

```text
テストランナー（PC 上で実行、printf を使用可）
        |
        | uart_comm_receive_char() を 1 文字ずつ呼び出す
        v
UART コマンド通信コア（stdio / Xilinx 非依存）
        |----------------------|----------------------|
        v                      v                      v
レジスタ操作スタブ        外部コマンドスタブ      出力ラッパースタブ
        |                      |                      |
        v                      v                      v
呼出し内容を記録           受信文字列と長さを記録  成功・エラー出力を記録
```

- `printf` はテストランナーおよびテストダブルだけで使用してよい。
- コアモジュールは、テスト時も `stdio` をインクルードせず、`printf` を呼び出さない。
- テストの合否は表示ログだけで判定してはならない。スタブに記録した呼び出し回数、
  アドレス、データ、文字列、長さおよび結果コードを期待値と比較して判定する。
- テストダブルは、指定された結果コードを返せるものとし、コールバック失敗時の
  結果伝播も検証可能にする。
- 出力ラッパースタブは、書き込み、読み出し、外部転送およびエラーの各出力内容と
  呼び出し回数を記録する。

## 4. 入力手順

1. テストごとに、既知のコールバック設定でコアモジュールを初期化する。
2. コマンド文字列の各文字を順番に `uart_comm_receive_char()` へ渡す。
3. 最後に行終端の `LF`（`0x0A`）または `CRLF`（`0x0D 0x0A`）を 1 文字ずつ
   渡す。
4. 行終端後、`uart_comm_has_pending_line()` が真であることを確認する。
5. `uart_comm_process_received_line()` を呼び出し、期待するコールバック、結果コード
   および出力ラッパーが 1 回だけ発生したことを確認する。
6. 次の行を入力し、前の行の状態が残らず継続して受信できることを確認する。

`uart_comm_process_line()` の直接テストは、受信バッファリングを介さない
コマンド解析の補助試験として実施してよい。ただし、公開された受信経路の確認には、
上記の 1 文字ずつの入力試験を必ず含める。

## 5. テスト項目

| ID | 試験内容 | 入力と確認内容 |
| --- | --- | --- |
| UT-01 | 初期化 | 行バッファと受信状態がクリアされ、設定したコールバックとコンテキストが使用されること。 |
| UT-02 | 受信と処理の分離 | `S 0000 1d\n` を1文字ずつ入力し、`LF` 後も `uart_comm_process_received_line()` を呼ぶまでレジスタ操作・出力が発生しないこと。 |
| UT-03 | 行終端 | `LF` と `CRLF` の双方で同一の行が完成し、コマンド行および外部本文に終端文字が含まれないこと。 |
| UT-04 | 空行 | `LF` のみを入力すると空行結果が通知され、レジスタおよび外部コールバックが呼ばれないこと。 |
| UT-05 | レジスタ書き込み（10進） | `S 0000 4d\n` により、書き込みスタブがアドレス `0x00000000`、値 `4` で1回呼ばれ、出力スタブが `=> S 0x00000000 4h\n` を記録すること。 |
| UT-06 | レジスタ書き込み（16進境界） | `S FFFFFFFF FFFFFFFFh\n` が成功し、アドレス・値とも `0xFFFFFFFF` となること。 |
| UT-07 | レジスタ読み出し | `L 0x0004 ignored\n` により、読み出しスタブがアドレス `0x00000004` で1回呼ばれ、第2引数を無視して `=> L 0x00000004 2ah\n` を出力すること。 |
| UT-08 | 外部コマンド転送とエコー | `T any string\n` により、外部スタブが `any string\n`（長さ11）を受け取り、成功時に出力スタブが `=> T any string\n` を記録すること。 |
| UT-09 | 空の外部コマンド | `T \n` で外部スタブを呼ばず、`UART_COMM_RESULT_MISSING_ARGUMENT` とエラーメッセージが出力されること。 |
| UT-10 | 不正コマンド | `X test\n` でレジスタおよび外部スタブを呼ばず、`UART_COMM_RESULT_INVALID_COMMAND` が出力されること。 |
| UT-11 | 数値および引数エラー | `S 0000 1\n`、`S 0000 -1d\n`、`S 0000 FFFFFFFFFh\n`、`S 0000 4294967296d\n`、`L 0004 x y\n` をそれぞれ入力し、仕様書の結果コードへ対応してアクセスを行わないこと。 |
| UT-12 | NUL 入力 | 行途中に `0x00` を含む入力で、終端まで破棄後に `UART_COMM_RESULT_INVALID_CHARACTER` を出力し、アクセスを行わないこと。 |
| UT-13 | `T` 本文の制御文字 | `T A<0x01>B<HT>C\n` により、外部スタブとエコー出力が `0x01` を除外し、`AB<HT>C\n` を受け取ること。 |
| UT-14 | 未設定・失敗コールバック | 必要なコールバック未設定時に `UART_COMM_RESULT_CONFIGURATION_ERROR`、各コールバックが失敗を返すと該当するアクセス／外部ハンドラエラーとなること。 |
| UT-15 | 行バッファ境界 | 256文字の行を受信・処理でき、257文字以上の行は `UART_COMM_RESULT_LINE_OVERFLOW` となること。 |
| UT-16 | 行オーバーフロー後の復帰 | オーバーフロー行の終端後に有効行を入力し、正常に処理できること。 |
| UT-17 | 2面FIFO | 処理関数を呼ばずに2行を完結させた後、処理関数を2回呼び出し、完結順に処理されること。 |
| UT-18 | 受信キューオーバーフロー | 2面が `READY` の状態で次の行を入力し、行終端まで破棄後に `UART_COMM_RESULT_RECEIVE_QUEUE_OVERFLOW` とエラーメッセージが1回だけ発生すること。 |

## 6. 合格基準

- 各テストケースで、期待された結果コード、結果通知回数およびコールバック呼び出し回数が一致すること。
- レジスタ操作スタブに記録されたアドレスとデータが期待値と一致すること。
- 外部コマンドスタブに記録された文字列、長さおよびコンテキストが期待値と一致すること。
- 不正入力およびオーバーフロー入力で、レジスタ操作または外部コマンド転送が一切発生しないこと。
- コアモジュールのソースとヘッダが `stdio`、Xilinx ヘッダおよび Xilinx API に依存しないことを、PC 向けテストビルドで確認すること。

## 7. 仕様上の前提

本試験仕様は `UART_COMM_SPEC.md` 版 0.6 の機能要件および非機能要件に整合する。
コアモジュールは単一インスタンスとし、PCテストでは排他コールバックを何もしない
関数として設定する。実機の UART 割り込みとの同時動作は、プラットフォームアダプタを
含む統合試験で確認する。
